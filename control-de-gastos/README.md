# 🎀 Control de Gastos

Aplicación web para registrar y controlar gastos personales. Permite agregar gastos por categoría, ver el total y el promedio, identificar en qué categoría se gasta más y guardar la información automáticamente en el navegador.

## ✨ Características

- ➕ Agregar gastos con categoría, descripción y monto
- 🗑️ Eliminar gastos de la lista
- 💸 Cálculo automático del total gastado
- 🌸 Cálculo del promedio por gasto
- 🏆 Detección de la categoría en la que más se gasta
- 💾 Guardado automático en el navegador (los datos no se pierden al recargar)
- ✅ Validación de datos (no permite categorías vacías ni montos inválidos)
- ⌨️ Permite agregar con la tecla Enter

## 🛠️ Tecnologías

- HTML5
- CSS3
- Bootstrap 5
- JavaScript

## 🚀 Cómo usar la aplicación

1. Descargar o clonar el proyecto.
2. Abrir el archivo `index.html` en el navegador (o usar Live Server en VS Code).
3. Al abrir, un mensaje de bienvenida pide tu nombre.
4. Completar categoría, descripción y monto, y presionar "Agregar ♡" (o Enter).
5. Los gastos aparecen en la tabla, junto con el total, el promedio y la categoría con más gasto.

## 📚 Conceptos de JavaScript aplicados

- Variables con `let` y `const`
- Arreglos y objetos (arreglo de objetos)
- Funciones con parámetros y retorno
- Funciones que se llaman entre sí
- Condicionales: `if`, `else`, `switch`
- Bucles: `for`, `for...of`
- Métodos de arreglos: `filter`, `some`, `includes`, `push`, `splice`
- Entrada y salida de datos: `prompt`, `alert`, `console.log`
- Manipulación del DOM
- Almacenamiento con `localStorage`
- Validación de datos ingresados por el usuario

## 👩‍💻 Autora

Javiera Saavedra ✨
